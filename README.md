# React Fashion Store

The runnable frontend app is located in the `FashionApp2/` folder.

## Quick Start

```bash
cd FashionApp2
npm install
npm run dev
```

Open `http://localhost:5173`.

For full app details, see `FashionApp2/README.md`.
